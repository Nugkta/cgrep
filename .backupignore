# Backup exclude patterns (rsync --exclude-from syntax).
# Skips git internals, archived/superseded material, and trained-model
# blobs. Data files are intentionally kept.
.git/
archive/
_archive/
wandb/
*.pt
*.pth
*.ckpt
*.safetensors
*.bin
*.h5
